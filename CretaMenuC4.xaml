<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:local="clr-namespace:CretaBase"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero">
    
	<Geometry x:Key="Checkmark">M 0,5.1 L 1.7,5.2 L 3.4,7.1 L 8,0.4 L 9.2,0 L 3.3,10.8 Z</Geometry>
    <Geometry x:Key="RightArrow">M 0,0 L 4,4 L 0,8</Geometry>

	<ControlTemplate x:Key="{ComponentResourceKey ResourceId=SubmenuContent, TypeInTargetAssembly={x:Type FrameworkElement}}" TargetType="{x:Type ContentControl}">
        <Border BorderBrush="{DynamicResource CretaprintC4_Blanco_br}" BorderThickness="1" Background="{DynamicResource CretaprintC4_Blanco_br}">
			<Grid>              
                <Rectangle Fill="#F1F1F1" HorizontalAlignment="Left" Margin="2" RadiusY="2" RadiusX="2" Width="28"/>
				<Rectangle Fill="#E2E3E3" HorizontalAlignment="Left" Margin="30,2,0,2" Width="1"/>
				<Rectangle Fill="White" HorizontalAlignment="Left" Margin="31,2,0,2" Width="1"/>
				<ContentPresenter Grid.ColumnSpan="2" Margin="1,0"/>
			</Grid>
		</Border>
	</ControlTemplate>
    
    <!-- Este template es el del menu CON submenus -->
    <ControlTemplate x:Key="{ComponentResourceKey ResourceId=TopLevelHeaderTemplateKey, TypeInTargetAssembly={x:Type MenuItem}}" TargetType="{x:Type MenuItem}">
		<Grid SnapsToDevicePixels="true">
			<!--<Path Data="M1,0 L1,90" HorizontalAlignment="Right" Margin="0,-1,1.5,-1" Stretch="Fill" Stroke="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}" StrokeThickness="1.5"/>-->
			<Path Data="M1,0 L1,90" HorizontalAlignment="Left" Margin="1.5,0,0,0" Stretch="Fill" Stroke="{DynamicResource CretaprintC4_Blanco_br}" StrokeThickness="1"/>
			<Rectangle x:Name="Bg" Fill="{TemplateBinding Background}" Margin="0" Stroke="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}" StrokeThickness="1"/>
			<Grid x:Name="GridBoton">
				<Grid.RowDefinitions>
					<RowDefinition Height="0.6*"/>
					<RowDefinition Height="0.4*"/>
				</Grid.RowDefinitions>
				<TextBlock x:Name="txtName" Text="{TemplateBinding Header}" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="15" Foreground="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}"/>
			</Grid>
            <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="0,0,0,20">
                <ContentPresenter x:Name="IconNegative" Visibility="Collapsed" ContentSource="Tag" Margin="0,0,0,0" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
                <ContentPresenter x:Name="Icon" ContentSource="Icon" Margin="0,0,0,0" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
                <Path x:Name="GlyphPanel" Data="{StaticResource Checkmark}" Fill="{TemplateBinding Foreground}" FlowDirection="LeftToRight" Margin="7,0,0,0" Visibility="Collapsed" VerticalAlignment="Center"/>
			</StackPanel>
            <!--PopUp del submenu principal-->
			<Popup x:Name="PART_Popup" AllowsTransparency="true" Focusable="false" HorizontalOffset="0" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" PopupAnimation="{DynamicResource {x:Static SystemParameters.MenuPopupAnimationKey}}" Placement="Bottom" VerticalOffset="0">
                <Microsoft_Windows_Themes:SystemDropShadowChrome x:Name="Shdw" Color="Transparent">
					<ContentControl x:Name="SubMenuBorder" IsTabStop="false" Template="{DynamicResource {ComponentResourceKey ResourceId=SubmenuContent, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                        <ContentControl.Effect>
                                <DropShadowEffect ShadowDepth="0" BlurRadius="10" Opacity="0.75"/>
                        </ContentControl.Effect>
                        <ScrollViewer x:Name="SubMenuScrollViewer" CanContentScroll="true" Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}}">
							<Grid RenderOptions.ClearTypeHint="Enabled">
								<Canvas HorizontalAlignment="Left" Height="0" VerticalAlignment="Top" Width="0">
                                    <Rectangle Fill="{DynamicResource CretaprintC4_Blanco_br}" Height="{Binding ActualHeight, ElementName=SubMenuBorder}" Width="{Binding ActualWidth, ElementName=SubMenuBorder}"/>
								</Canvas>
								<ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Cycle" Grid.IsSharedSizeScope="true" Margin="2" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" KeyboardNavigation.TabNavigation="Cycle"/>
							</Grid>
						</ScrollViewer>
					</ContentControl>
				</Microsoft_Windows_Themes:SystemDropShadowChrome>
			</Popup>			
		</Grid>
		<ControlTemplate.Triggers>
			<Trigger Property="IsSuspendingPopupAnimation" Value="true">
				<Setter Property="PopupAnimation" TargetName="PART_Popup" Value="None"/>
			</Trigger>
			<Trigger Property="Icon" Value="{x:Null}">
				<Setter Property="Visibility" TargetName="Icon" Value="Collapsed"/>
			</Trigger>
            <Trigger Property="Tag" Value="{x:Null}">
                <Setter Property="Visibility" TargetName="IconNegative" Value="Collapsed"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="true">
				<Setter Property="Visibility" TargetName="GlyphPanel" Value="Visible"/>
				<Setter Property="Visibility" TargetName="Icon" Value="Collapsed"/>
			</Trigger>
			<Trigger Property="HasDropShadow" SourceName="PART_Popup" Value="true">
				<Setter Property="Margin" TargetName="Shdw" Value="10"/>
				<Setter Property="Color" TargetName="Shdw" Value="#00000000"/>
			</Trigger>
			<Trigger Property="IsHighlighted" Value="true">
				<Setter Property="Stroke" TargetName="Bg" Value="#90717070"/>
				<Setter Property="Background" TargetName="GridBoton" Value="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}"/>
				<Setter Property="Foreground" TargetName="txtName" Value="{DynamicResource CretaprintC4_Blanco_br}" />
                <Setter Property="Visibility" TargetName="Icon" Value="Collapsed" />
                <Setter Property="Visibility" TargetName="IconNegative" Value="Visible" />
            </Trigger>
			<Trigger Property="IsKeyboardFocused" Value="true">
				<Setter Property="Stroke" TargetName="Bg" Value="#E0717070"/>
			</Trigger>
			<Trigger Property="IsSubmenuOpen" Value="true">
				<Setter Property="Stroke" TargetName="Bg" Value="#E0717070"/>
			</Trigger>
			<Trigger Property="IsEnabled" Value="false">
                <Setter Property="Foreground" TargetName="txtName" Value="{DynamicResource CretaprintC4_GrisClaro_Barras_br}"/>
				<Setter Property="Foreground" Value="#FF9A9A9A"/>
				<Setter Property="Fill" TargetName="GlyphPanel" Value="#848589"/>
			</Trigger>
		</ControlTemplate.Triggers>
	</ControlTemplate>
    <!-- Este template es el del menu SIN submenus -->
	<ControlTemplate x:Key="{ComponentResourceKey ResourceId=TopLevelItemTemplateKey, TypeInTargetAssembly={x:Type MenuItem}}" TargetType="{x:Type MenuItem}">
		<Grid SnapsToDevicePixels="true">
			<VisualStateManager.VisualStateGroups>
				<VisualStateGroup x:Name="ValidationStates">
					<VisualState x:Name="Valid"/>
					<VisualState x:Name="InvalidFocused"/>
					<VisualState x:Name="InvalidUnfocused"/>
				</VisualStateGroup>
			</VisualStateManager.VisualStateGroups>
			<Path Data="M1,0 L1,90" HorizontalAlignment="Left" Margin="1.5,-1,0,-1" Stretch="Fill" Stroke="{DynamicResource CretaprintC4_Blanco_br}" StrokeThickness="1"/>
			<Rectangle x:Name="Bg" Fill="{TemplateBinding Background}" Margin="0" Stroke="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}" StrokeThickness="1"/>
			<Grid x:Name="GridBoton">
				<Grid.RowDefinitions>
					<RowDefinition Height="0.6*"/>
					<RowDefinition Height="0.4*"/>
				</Grid.RowDefinitions>
				<TextBlock x:Name="txtName" Text="{TemplateBinding Header}" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="15" Foreground="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}"/>
			</Grid>
			<StackPanel Orientation="Vertical" VerticalAlignment="Center" Grid.Row="1">
                <ContentPresenter x:Name="IconNegative" Visibility="Collapsed" ContentSource="Tag" Margin="0,0,0,0" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
				<ContentPresenter x:Name="Icon" ContentSource="Icon" Margin="4,0,6,0" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
				<Path x:Name="GlyphPanel" Data="{StaticResource Checkmark}" Fill="{TemplateBinding Foreground}" FlowDirection="LeftToRight" Margin="7,0,0,0" Visibility="Collapsed" VerticalAlignment="Center"/>
			</StackPanel>
		</Grid>
		<ControlTemplate.Triggers>
			<Trigger Property="Icon" Value="{x:Null}">
				<Setter Property="Visibility" TargetName="Icon" Value="Collapsed"/>
			</Trigger>
            <Trigger Property="Tag" Value="{x:Null}">
                <Setter Property="Visibility" TargetName="IconNegative" Value="Collapsed"/>
            </Trigger>
			<Trigger Property="IsChecked" Value="true">
				<Setter Property="Visibility" TargetName="GlyphPanel" Value="Visible"/>
				<Setter Property="Visibility" TargetName="Icon" Value="Collapsed"/>
			</Trigger>
			<Trigger Property="IsHighlighted" Value="true">
				<Setter Property="Stroke" TargetName="Bg" Value="#90717070"/>
				<Setter Property="Background" TargetName="GridBoton" Value="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}"/>
				<Setter Property="Foreground" TargetName="txtName" Value="{DynamicResource CretaprintC4_Blanco_br}" />
                <Setter Property="Visibility" TargetName="Icon" Value="Collapsed" />
                <Setter Property="Visibility" TargetName="IconNegative" Value="Visible" />
			</Trigger>
			<Trigger Property="IsKeyboardFocused" Value="true">
				<Setter Property="Stroke" TargetName="Bg" Value="#E0717070"/>
            </Trigger>
			<Trigger Property="IsEnabled" Value="false">
                <Setter Property="Foreground" TargetName="txtName" Value="{DynamicResource CretaprintC4_GrisClaro_Barras_br}"/>
				<Setter Property="Foreground" Value="#FF9A9A9A"/>
				<Setter Property="Fill" TargetName="GlyphPanel" Value="#848589"/>
			</Trigger>
		</ControlTemplate.Triggers>
	</ControlTemplate>
    <!-- Este template es el del submenu SIN hijos -->
    <ControlTemplate x:Key="{ComponentResourceKey ResourceId=SubmenuItemTemplateKey, TypeInTargetAssembly={x:Type MenuItem}}" TargetType="{x:Type MenuItem}">
        <Grid SnapsToDevicePixels="true" Width="420" Height="64">
            <Rectangle x:Name="Bg" Fill="{DynamicResource CretaprintC4_Blanco_br}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition MinWidth="24" SharedSizeGroup="MenuItemIconColumnGroup" Width="64"/>
                    <ColumnDefinition Width="4"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="37"/>
                    <ColumnDefinition SharedSizeGroup="MenuItemIGTColumnGroup" Width="Auto"/>
                    <ColumnDefinition Width="17"/>
                </Grid.ColumnDefinitions>
                <ContentPresenter x:Name="IconNegative" ContentSource="Tag" Visibility="Collapsed" Margin="1" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
                <ContentPresenter x:Name="Icon" ContentSource="Icon" Margin="1" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
                <Border x:Name="GlyphPanel2" BorderBrush="#CDD3E6" BorderThickness="1" Background="#E6EFF4" CornerRadius="3" Height="22" Margin="1" Visibility="Hidden" Width="22">
                    <Path x:Name="Glyph" Data="{StaticResource Checkmark}" Fill="#0C12A1" FlowDirection="LeftToRight" Height="11" Width="9"/>
                </Border>
				<Grid x:Name="CheckGlyphs">
					<ContentControl x:Name="CheckOK" Style="{DynamicResource Check_Ok}" Foreground="{DynamicResource CretaprintC4_Verde_br}" local:CretaProperties.Scale="2" Margin="7,0,0,0" Visibility="Collapsed" VerticalAlignment="Center"/>
					<ContentControl x:Name="CheckBad" Style="{DynamicResource Check_Bad}" Foreground="{DynamicResource CretaprintC4_Rojo_Claro_br}" local:CretaProperties.Scale="2" Margin="3,0,0,0" Visibility="Collapsed" VerticalAlignment="Center"/>
				</Grid>
                <TextBlock x:Name="txtName" Text="{TemplateBinding Header}" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="15" Foreground="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}"/>
                <!--<ContentPresenter Grid.Column="2" ContentSource="Header" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>-->
                <TextBlock x:Name="txtInputGest" Grid.Column="4" Margin="{TemplateBinding Padding}" Text="{TemplateBinding InputGestureText}" VerticalAlignment="Center" FontSize="15" Foreground="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}"/>
            </Grid>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="Icon" Value="{x:Null}">
                <Setter Property="Visibility" TargetName="Icon" Value="Collapsed"/>
            </Trigger>
            <Trigger Property="Tag" Value="{x:Null}">
                <Setter Property="Visibility" TargetName="IconNegative" Value="Collapsed"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="true">
                <Setter Property="Visibility" TargetName="CheckOK" Value="Visible"/>
                <Setter Property="Visibility" TargetName="Icon" Value="Collapsed"/>
            </Trigger>
			<Trigger Property="IsCheckable" Value="false">
                <Setter Property="Visibility" TargetName="CheckGlyphs" Value="Collapsed"/>
            </Trigger>
			<Trigger Property="IsChecked" Value="false">
                <Setter Property="Visibility" TargetName="CheckBad" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsHighlighted" Value="true">
                <Setter Property="Fill" TargetName="Bg" Value="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}"/>
                <Setter Property="Stroke" TargetName="Bg" Value="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}"/>
                <Setter Property="Foreground" TargetName="txtName" Value="{DynamicResource CretaprintC4_Blanco_br}" />
                <Setter Property="Foreground" TargetName="txtInputGest" Value="{DynamicResource CretaprintC4_Blanco_br}" />
                <Setter Property="Visibility" TargetName="Icon" Value="Collapsed" />
                <Setter Property="Visibility" TargetName="IconNegative" Value="Visible" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Foreground" TargetName="txtName" Value="{DynamicResource CretaprintC4_GrisClaro_Barras_br}"/>
                <!--<Setter Property="Background" TargetName="GlyphPanel" Value="#EEE9E9"/>-->
                <!--<Setter Property="BorderBrush" TargetName="GlyphPanel" Value="#DBD6D6"/>-->
                <Setter Property="Fill" TargetName="Glyph" Value="#848589"/>
				<Setter Property="Opacity" TargetName="Icon" Value="0.25"/>
				<Setter Property="Opacity" TargetName="IconNegative" Value="0.25"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <!-- Este template es el del submenu CON hijos -->
    <ControlTemplate x:Key="{ComponentResourceKey ResourceId=SubmenuHeaderTemplateKey, TypeInTargetAssembly={x:Type MenuItem}}" TargetType="{x:Type MenuItem}">
        <Grid SnapsToDevicePixels="true" Width="420" Height="64">
            <Rectangle x:Name="Bg" Fill="{DynamicResource CretaprintC4_Blanco_br}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition MinWidth="24" SharedSizeGroup="MenuItemIconColumnGroup" Width="64"/>
                    <ColumnDefinition Width="4"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="37"/>
                    <ColumnDefinition SharedSizeGroup="MenuItemIGTColumnGroup" Width="Auto"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <ContentPresenter x:Name="IconNegative" ContentSource="Tag" Visibility="Collapsed" Margin="1" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
                <ContentPresenter x:Name="Icon" ContentSource="Icon" Margin="1" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
                <Border x:Name="GlyphPanel" BorderBrush="#CDD3E6" BorderThickness="1" Background="#E6EFF4" CornerRadius="3" Height="22" Margin="1" Visibility="Hidden" Width="22">
                    <Path x:Name="Glyph" Data="{StaticResource Checkmark}" Fill="#0C12A1" FlowDirection="LeftToRight" Height="11" Width="9"/>
                </Border>
                <!--<ContentPresenter Grid.Column="2" ContentSource="Header" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>-->
                <TextBlock x:Name="txtName" Text="{TemplateBinding Header}" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="15" Foreground="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}"/>
                <TextBlock Grid.Column="4" Margin="{TemplateBinding Padding}" Text="{TemplateBinding InputGestureText}" Visibility="Collapsed"/>
                <Path Grid.Column="5" Data="{StaticResource RightArrow}" Stroke="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}" Width="12" Height="24" Stretch="Fill" Margin="4" VerticalAlignment="Center"/>
            </Grid>
            <Popup x:Name="PART_Popup" AllowsTransparency="true" Focusable="false" HorizontalOffset="0" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" PopupAnimation="{DynamicResource {x:Static SystemParameters.MenuPopupAnimationKey}}" Placement="Right" VerticalOffset="-3">
                <Microsoft_Windows_Themes:SystemDropShadowChrome x:Name="Shdw" Color="Transparent">
                    <ContentControl x:Name="SubMenuBorder" IsTabStop="false" Template="{DynamicResource {ComponentResourceKey ResourceId=SubmenuContent, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                        <ContentControl.Effect>
                            <DropShadowEffect ShadowDepth="0" BlurRadius="10" Opacity="0.75"/>
                        </ContentControl.Effect>
                        <ScrollViewer x:Name="SubMenuScrollViewer" CanContentScroll="true" Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                            <Grid RenderOptions.ClearTypeHint="Enabled">
                                <Canvas HorizontalAlignment="Left" Height="0" VerticalAlignment="Top" Width="0">
                                    <Rectangle Fill="{DynamicResource CretaprintC4_Blanco_br}" Height="{Binding ActualHeight, ElementName=SubMenuBorder}" Width="{Binding ActualWidth, ElementName=SubMenuBorder}"/>
                                </Canvas>
                                <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Cycle" Grid.IsSharedSizeScope="true" Margin="2" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" KeyboardNavigation.TabNavigation="Cycle"/>
                            </Grid>
                        </ScrollViewer>
                    </ContentControl>
                </Microsoft_Windows_Themes:SystemDropShadowChrome>
            </Popup>

        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsSuspendingPopupAnimation" Value="true">
                <Setter Property="PopupAnimation" TargetName="PART_Popup" Value="None"/>
            </Trigger>
            <Trigger Property="IsHighlighted" Value="true">
            </Trigger>
            <Trigger Property="Icon" Value="{x:Null}">
                <Setter Property="Visibility" TargetName="Icon" Value="Collapsed"/>
            </Trigger>
            <Trigger Property="Tag" Value="{x:Null}">
                <Setter Property="Visibility" TargetName="IconNegative" Value="Collapsed"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="true">
                <Setter Property="Visibility" TargetName="GlyphPanel" Value="Visible"/>
                <Setter Property="Visibility" TargetName="Icon" Value="Collapsed"/>
            </Trigger>
            <Trigger Property="HasDropShadow" SourceName="PART_Popup" Value="true">
                <Setter Property="Margin" TargetName="Shdw" Value="10"/>
                <Setter Property="Color" TargetName="Shdw" Value="#00000000"/>
            </Trigger>
            <Trigger Property="IsHighlighted" Value="true">
                <Setter Property="Fill" TargetName="Bg" Value="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}"/>
                <Setter Property="Stroke" TargetName="Bg" Value="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}"/>
                <Setter Property="Foreground" TargetName="txtName" Value="{DynamicResource CretaprintC4_Blanco_br}" />
                <Setter Property="Visibility" TargetName="Icon" Value="Collapsed" />
                <Setter Property="Visibility" TargetName="IconNegative" Value="Visible" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Foreground" TargetName="txtName" Value="{DynamicResource CretaprintC4_GrisClaro_Barras_br}"/>
                <Setter Property="Background" TargetName="GlyphPanel" Value="#EEE9E9"/>
                <Setter Property="BorderBrush" TargetName="GlyphPanel" Value="#DBD6D6"/>
                <Setter Property="Fill" TargetName="Glyph" Value="#848589"/>
				<Setter Property="Opacity" TargetName="Icon" Value="0.25"/>
				<Setter Property="Opacity" TargetName="IconNegative" Value="0.25"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="CretaMenuSeparator" TargetType="{x:Type Separator}">
		<Grid Height="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="64"/>
				<ColumnDefinition />
			</Grid.ColumnDefinitions>
			<Grid Grid.Column="1" Background="#FFcccccc"/>
		</Grid>
	</ControlTemplate>
	<ControlTemplate x:Key="CretaMenuSeparatorBig" TargetType="{x:Type Separator}">
		<Grid Height="5" Background="#FFcccccc">
		</Grid>
	</ControlTemplate>
</ResourceDictionary>