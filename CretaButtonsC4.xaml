<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	xmlns:local="clr-namespace:CretaBase"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">
	<Style x:Key="ButtonFocusVisual">
		<Setter Property="Control.Template">
			<Setter.Value>
				<ControlTemplate>
					<Rectangle Margin="0" SnapsToDevicePixels="true" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" StrokeThickness="1" StrokeDashArray="1 2"/>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
	<SolidColorBrush x:Key="ButtonNormalBorder" Color="#FF707070"/>
    <Style x:Key="CretaButtonTxtBlockC4" TargetType="{x:Type Button}">
        <!-- <Setter Property="FocusVisualStyle" Value="{DynamicResource CretaFocusVisualStyle}" /> -->
        <Setter Property="Background" Value="{StaticResource BotonDegradadoGris}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ButtonNormalBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid x:Name="Btn_tab" Cursor="Hand" Height="Auto" VerticalAlignment="Stretch" Width="auto" HorizontalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="0.5*"/>
                            <RowDefinition Height="0.5*"/>
                        </Grid.RowDefinitions>
                        <Rectangle x:Name="Bg_Negro" Margin="0" RadiusX="5" RadiusY="5" Grid.RowSpan="2" Fill="Black" Visibility="Collapsed"/>
                        <Rectangle x:Name="Bg" Margin="0" RadiusX="5" RadiusY="5" Grid.RowSpan="2" Stroke="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" StrokeThickness="1">
                            <Rectangle.Fill>
                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                    <GradientStop Color="White" Offset="0"/>
                                    <GradientStop Color="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" Offset="0.5"/>
                                </LinearGradientBrush>
                            </Rectangle.Fill>
                        </Rectangle>
                        <ContentPresenter HorizontalAlignment="Center" Margin="0" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center" Grid.RowSpan="2"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Tag" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Visible" TargetName="Bg_Negro"/>
                            <Setter Property="Visibility" Value="Collapsed" TargetName="Bg"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="true"/>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Fill" TargetName="Bg" >
                                <Setter.Value>
                                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                        <GradientStop Color="White" Offset="-0.5"/>
                                        <GradientStop Color="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" Offset="0.5"/>
                                        <GradientStop Color="White" Offset="1.5"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="local:CretaProperties.IsButtonSelected" Value="true">
                            <Setter Property="IsEnabled" TargetName="Btn_tab" Value="False"/>
                            <Setter Property="Fill" TargetName="Bg">
                                <Setter.Value>
                                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                        <GradientStop Color="White" Offset="-0.5"/>
                                        <GradientStop Color="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" Offset="0.5"/>
                                        <GradientStop Color="White" Offset="1.5"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" Value="0.25"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
	<Style x:Key="CretaButtonTabC4" TargetType="{x:Type Button}">
        <!-- <Setter Property="FocusVisualStyle" Value="{DynamicResource CretaFocusVisualStyle}" /> -->
        <Setter Property="Background" Value="{StaticResource BotonDegradadoGris}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ButtonNormalBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid x:Name="Btn_tab" Cursor="Hand" Height="Auto" VerticalAlignment="Stretch" Width="auto" HorizontalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="0.5*"/>
                            <RowDefinition Height="0.5*"/>
                        </Grid.RowDefinitions>
                        <Rectangle x:Name="Bg_Negro" Margin="0" RadiusX="5" RadiusY="5" Grid.RowSpan="2" Fill="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}" />
                        <Rectangle x:Name="Bg" Margin="0" RadiusX="5" RadiusY="5" Grid.RowSpan="2" Fill="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" />
                        <ContentPresenter HorizontalAlignment="Center" Margin="0" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center" Grid.RowSpan="2"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Tag" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Visible" TargetName="Bg_Negro"/>
                            <Setter Property="Visibility" Value="Collapsed" TargetName="Bg"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="true"/>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Fill" TargetName="Bg_Negro" Value="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}" />
                        </Trigger>
                        <Trigger Property="local:CretaProperties.IsButtonSelected" Value="true">
                            <Setter Property="IsEnabled" TargetName="Btn_tab" Value="False"/>
                            <Setter Property="Fill" TargetName="Bg_Negro" Value="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}"/>
							<Setter Property="Fill" TargetName="Bg" Value="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" Value="0.25"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
	
    <Style x:Key="CretaButtonRoundC4" TargetType="{x:Type Button}">
		<Setter Property="FocusVisualStyle" Value="{StaticResource ButtonFocusVisual}"/>
		<Setter Property="BorderBrush" Value="{StaticResource ButtonNormalBorder}"/>
		<Setter Property="Background" Value="{StaticResource CretaprintC4_GrisOscuro_Fondo_br}"/>
		<Setter Property="BorderThickness" Value="1"/>
		<Setter Property="Foreground" Value="White"/>
		<Setter Property="HorizontalContentAlignment" Value="Center"/>
		<Setter Property="VerticalContentAlignment" Value="Center"/>
		<Setter Property="Padding" Value="1"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type Button}">
                    <Grid Cursor="Hand" x:Name="GridButton">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="0.2*"/>
							<ColumnDefinition Width="0.6*"/>
							<ColumnDefinition Width="0.2*"/>
						</Grid.ColumnDefinitions>
						<Grid.RowDefinitions>
							<RowDefinition Height="0.6*"/>
							<RowDefinition Height="0.4*"/>
						</Grid.RowDefinitions>
						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal"/>
								<VisualState x:Name="MouseOver"/>
								<VisualState x:Name="Pressed">
									<Storyboard>
										<DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" Storyboard.TargetName="FondoElipse">
											<EasingDoubleKeyFrame KeyTime="0" Value="0.95"/>
										</DoubleAnimationUsingKeyFrames>
										<DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" Storyboard.TargetName="FondoElipse">
											<EasingDoubleKeyFrame KeyTime="0" Value="0.95"/>
										</DoubleAnimationUsingKeyFrames>										
									</Storyboard>
								</VisualState>
								<VisualState x:Name="Disabled"/>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>
						<Rectangle x:Name="AreaSeleccionable" Stroke="Transparent" Fill="Transparent" Grid.Column="0" Grid.ColumnSpan="3" Grid.RowSpan="3" Margin="5,0"/>
						<Ellipse x:Name="FondoElipse" Margin="0" Stroke="{x:Null}" Grid.Column="1" Fill="{TemplateBinding Background}" RenderTransformOrigin="0.5,0.5">
							<Ellipse.RenderTransform>
								<TransformGroup>
									<ScaleTransform/>
									<SkewTransform/>
									<RotateTransform/>
									<TranslateTransform/>
								</TransformGroup>
							</Ellipse.RenderTransform>						
						</Ellipse>
						<ContentControl Focusable="False" x:Name="Contenido" HorizontalAlignment="Center" Margin="0" 
							Content="{TemplateBinding Content}" 
							VerticalAlignment="Center" Grid.Column="1" />
						<ContentControl Focusable="False" x:Name="Contenido2" HorizontalAlignment="Center" Margin="0" 
							Content="{Binding Path=(local:CretaProperties.Content2), RelativeSource={RelativeSource AncestorType={x:Type ContentControl}}}" 
							VerticalAlignment="Center" Grid.Column="1" />
						<TextBlock x:Name="Texto" Text="{TemplateBinding Tag}" TextAlignment="Center" Grid.Row="2" FontFamily="Arial" HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap" Foreground="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}" Grid.ColumnSpan="3" Margin="0,0,0,2" d:LayoutOverrides="GridBox"/>
					</Grid>
					<ControlTemplate.Triggers>
						<!--<Trigger Property="IsKeyboardFocused" Value="true">
							<Setter Property="Stroke" TargetName="AreaSeleccionable" Value="#FF000000"/>
						</Trigger>-->
						<Trigger Property="IsMouseOver" Value="true">
							<Setter Property="Fill" TargetName="FondoElipse" Value="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}"/>
							<Setter Property="Visibility" TargetName="Contenido" Value="Collapsed"/>
							<Setter Property="Visibility" TargetName="Contenido2" Value="Visible"/>
							<Setter Property="Foreground" TargetName="Texto" Value="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}"/>
						</Trigger>
						<Trigger Property="IsMouseOver" Value="false">
							<Setter Property="Visibility" TargetName="Contenido" Value="Visible"/>
							<Setter Property="Visibility" TargetName="Contenido2" Value="Collapsed"/>
						</Trigger>
						<Trigger Property="IsEnabled" Value="false">
							<Setter Property="Opacity" Value="0.25"/>
						</Trigger>
                        <Trigger Property="local:CretaProperties.IsButtonSelected" Value="true">
                            <Setter Property="Fill" TargetName="FondoElipse" Value="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}"/>
                            <Setter Property="Visibility" TargetName="Contenido" Value="Collapsed"/>
                            <Setter Property="Visibility" TargetName="Contenido2" Value="Visible"/>
                            <Setter Property="IsEnabled" TargetName="GridButton" Value="False"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
	<Style x:Key="CretaButtonRoundLightC4" TargetType="{x:Type Button}">
		<Setter Property="FocusVisualStyle" Value="{StaticResource ButtonFocusVisual}"/>
        <Setter Property="Background" Value="{StaticResource BotonDegradadoGris2}"/>
		<Setter Property="BorderBrush" Value="{StaticResource ButtonNormalBorder}"/>
		<Setter Property="BorderThickness" Value="1"/>
		<Setter Property="HorizontalContentAlignment" Value="Center"/>
		<Setter Property="VerticalContentAlignment" Value="Center"/>
		<Setter Property="Padding" Value="1"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type Button}">
					<Grid x:Name="GridTodo" Cursor="Hand">
						<Ellipse x:Name="FondoElipse" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0" Fill="{TemplateBinding Background}"/>
						<!--<ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>-->
						<TextBlock x:Name="Texto" Text="{TemplateBinding Tag}" TextAlignment="Center" FontFamily="Lucida" 
								HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="28" FontWeight="Bold"
								Foreground="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}" />
						<ContentControl Focusable="False" x:Name="Contenido" HorizontalAlignment="Center" Margin="0" 
								Content="{TemplateBinding Content}" 
								VerticalAlignment="Center" Grid.Column="1" />
						<ContentControl Focusable="False" x:Name="Contenido2" HorizontalAlignment="Center" Margin="0" 
								Content="{Binding Path=(local:CretaProperties.Content2), RelativeSource={RelativeSource AncestorType={x:Type ContentControl}}}" 
								VerticalAlignment="Center" Grid.Column="1" />
					</Grid>
					
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="true">
							<Setter Property="Fill" TargetName="FondoElipse" Value="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}"/>
							<Setter Property="Visibility" TargetName="Contenido" Value="Collapsed"/>
							<Setter Property="Visibility" TargetName="Contenido2" Value="Visible"/>
							<Setter Property="Foreground" TargetName="Texto" Value="White"/>
						</Trigger>
						<Trigger Property="IsMouseOver" Value="false">
							<Setter Property="Visibility" TargetName="Contenido" Value="Visible"/>
							<Setter Property="Visibility" TargetName="Contenido2" Value="Collapsed"/>
						</Trigger>
						<Trigger Property="IsEnabled" Value="false">
							<Setter Property="Opacity" Value="0.25"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
    <Style x:Key="CretaButtonRoundBigC4" TargetType="{x:Type Button}">
        <Setter Property="FocusVisualStyle" Value="{StaticResource ButtonFocusVisual}"/>
        <Setter Property="Background" Value="{StaticResource BotonDegradadoGris}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ButtonNormalBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid x:Name="GridTodo" Cursor="Hand">
                    	<Grid.RowDefinitions>
                    		<RowDefinition Height="20*"/>
                    		<RowDefinition Height="40*"/>
                    		<RowDefinition Height="40*"/>
                    	</Grid.RowDefinitions>
                        <Ellipse x:Name="FondoElipse" Margin="0" Stroke="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}" Grid.Column="0" Fill="{TemplateBinding Background}" Grid.RowSpan="3"/>
                        <ContentControl Focusable="False" x:Name="Contenido" HorizontalAlignment="Center" Margin="0" 
								Content="{TemplateBinding Content}" 
								VerticalAlignment="Center" Grid.Row="1"/>
						<ContentControl Focusable="False" x:Name="Contenido2" HorizontalAlignment="Center" Margin="0"
								Content="{Binding Path=(local:CretaProperties.Content2), RelativeSource={RelativeSource AncestorType={x:Type ContentControl}}}" 
								VerticalAlignment="Center" Grid.Row="1" />
                        <TextBlock x:Name="Texto" Text="{TemplateBinding Tag}" TextAlignment="Center" Grid.Row="2" FontFamily="Arial" 
								HorizontalAlignment="Center" VerticalAlignment="Top" TextWrapping="Wrap" FontSize="15" 
								Foreground="White" Margin="0,0,0,0"/>
					</Grid>
					<ControlTemplate.Triggers>
						<!--<Trigger Property="IsKeyboardFocused" Value="true">
							<Setter Property="Stroke" TargetName="AreaSeleccionable" Value="#FF000000"/>
						</Trigger>-->
						<Trigger Property="IsMouseOver" Value="true">
							<Setter Property="Fill" TargetName="FondoElipse" Value="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}"/>
							<Setter Property="Visibility" TargetName="Contenido" Value="Collapsed"/>
							<Setter Property="Visibility" TargetName="Contenido2" Value="Visible"/>
							<Setter Property="Foreground" TargetName="Texto" Value="White"/>
						</Trigger>
						<Trigger Property="IsMouseOver" Value="false">
							<Setter Property="Visibility" TargetName="Contenido" Value="Visible"/>
							<Setter Property="Visibility" TargetName="Contenido2" Value="Collapsed"/>
						</Trigger>
						<Trigger Property="IsEnabled" Value="false">
							<Setter Property="Opacity" Value="0.25"/>
						</Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="CretaButtonSquareC4" TargetType="{x:Type Button}">
		<Setter Property="FocusVisualStyle" Value="{StaticResource ButtonFocusVisual}"/>
		<Setter Property="BorderBrush" Value="{StaticResource ButtonNormalBorder}"/>
		<Setter Property="Background" Value="{StaticResource BotonDegradadoGris}"/>
		<Setter Property="BorderThickness" Value="1"/>
		<Setter Property="Foreground" Value="White"/>
		<Setter Property="HorizontalContentAlignment" Value="Center"/>
		<Setter Property="VerticalContentAlignment" Value="Center"/>
		<Setter Property="Padding" Value="1"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type Button}">
					<Grid x:Name="GridButton" Cursor="Hand">
						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal"/>
								<VisualState x:Name="MouseOver"/>
								<VisualState x:Name="Pressed"/>
								<VisualState x:Name="Disabled"/>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>
						<Rectangle x:Name="AreaSeleccionable" Stroke="Transparent" Fill="Transparent" Grid.RowSpan="2"/>
						<StackPanel Orientation="Vertical">
							<Grid>
								<Rectangle Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" 
									x:Name="FondoCuadrado" Margin="0" Stroke="{x:Null}" RadiusX="15" RadiusY="15" Fill="{TemplateBinding Background}">
									<!--<Rectangle.Fill>
										<RadialGradientBrush RadiusY="1.042" RadiusX="0.972" Center="-0.287,-0.239" GradientOrigin="-0.551,-0.42">
											<GradientStop Color="{DynamicResource CretaprintC4_GrisClaro_Trazos}" Offset="1"/>
											<GradientStop Color="White"/>
										</RadialGradientBrush>
									</Rectangle.Fill>-->
								</Rectangle>
								<ContentControl Focusable="False" x:Name="Contenido" HorizontalAlignment="Center" Margin="0" 
									Content="{TemplateBinding Content}" 
									VerticalAlignment="Center" />
								<ContentControl Focusable="False" x:Name="Contenido2" HorizontalAlignment="Center" Margin="0" 
									Content="{Binding Path=(local:CretaProperties.Content2), RelativeSource={RelativeSource AncestorType={x:Type ContentControl}}}" 
									VerticalAlignment="Center" />
							</Grid>
							<TextBlock Text="{TemplateBinding Tag}" x:Name="Texto" TextAlignment="Center" Grid.Row="1" FontFamily="Arial" HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap" Foreground="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}" Margin="0,10,0,0" Height="auto"/>
						</StackPanel>
					</Grid>
					<ControlTemplate.Triggers>
						<!--<Trigger Property="IsKeyboardFocused" Value="true">
							<Setter Property="Stroke" TargetName="AreaSeleccionable" Value="#FF000000"/>
						</Trigger>-->
						<Trigger Property="IsMouseOver" Value="true">
							<Setter Property="Fill" TargetName="FondoCuadrado" Value="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}"/>
							<Setter Property="Visibility" TargetName="Contenido" Value="Collapsed"/>
							<Setter Property="Visibility" TargetName="Contenido2" Value="Visible"/>
							<Setter Property="Foreground" TargetName="Texto" Value="Black"/>
						</Trigger>
						<Trigger Property="IsMouseOver" Value="false">
							<Setter Property="Visibility" TargetName="Contenido" Value="Visible"/>
							<Setter Property="Visibility" TargetName="Contenido2" Value="Collapsed"/>
						</Trigger>
						<Trigger Property="IsEnabled" Value="false">
							<Setter Property="Opacity" Value="0.25"/>
						</Trigger>
                        <Trigger Property="local:CretaProperties.IsButtonSelected" Value="true">
                            <Setter Property="Fill" TargetName="FondoCuadrado" Value="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}"/>
                            <Setter Property="Visibility" TargetName="Contenido" Value="Collapsed"/>
                            <Setter Property="Visibility" TargetName="Contenido2" Value="Visible"/>
                            <Setter Property="IsEnabled" TargetName="GridButton" Value="False"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
	<Style x:Key="CretaButtonSquareLightC4" TargetType="{x:Type Button}">
		<Setter Property="FocusVisualStyle" Value="{StaticResource ButtonFocusVisual}"/>
		<Setter Property="BorderBrush" Value="{StaticResource ButtonNormalBorder}"/>
		<Setter Property="Background" Value="{StaticResource BotonDegradadoGris}"/>
		<Setter Property="BorderThickness" Value="1"/>
		<Setter Property="Foreground" Value="White"/>
		<Setter Property="HorizontalContentAlignment" Value="Center"/>
		<Setter Property="VerticalContentAlignment" Value="Center"/>
		<Setter Property="Padding" Value="1"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type Button}">
					<Grid x:Name="GridButton" Cursor="Hand">
						<Rectangle x:Name="AreaSeleccionable" Stroke="Transparent" Fill="Transparent" Grid.RowSpan="2"/>
							<Grid>
								<Rectangle Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" 
									x:Name="FondoCuadrado" Margin="0" Stroke="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}" Fill="{TemplateBinding Background}">
								</Rectangle>
								<ContentControl Focusable="False" x:Name="Contenido" HorizontalAlignment="Center" Margin="0,0,0,15" 
									Content="{TemplateBinding Content}" 
									VerticalAlignment="Center" />
								<TextBlock Text="{TemplateBinding Tag}" x:Name="Texto" TextAlignment="Center" FontFamily="Arial" HorizontalAlignment="Center" VerticalAlignment="Bottom" TextWrapping="Wrap" Foreground="White" Margin="5" Height="auto"/>
							</Grid>
					</Grid>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="true">
							<Setter Property="Fill" TargetName="FondoCuadrado" Value="{DynamicResource CretaprintC4_GrisOscuro_Seleccion_br}"/>
						</Trigger>
						<Trigger Property="IsEnabled" Value="false">
							<Setter Property="Opacity" Value="0.25"/>
						</Trigger>
                    </ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
	
	<Style x:Key="CretaButtonContentC4" TargetType="{x:Type Button}">
		<Setter Property="FocusVisualStyle" Value="{StaticResource ButtonFocusVisual}"/>
		<Setter Property="HorizontalContentAlignment" Value="Center"/>
		<Setter Property="VerticalContentAlignment" Value="Center"/>
		<Setter Property="Padding" Value="1"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type Button}">
					<Grid x:Name="GridButton" Cursor="Hand">
						<ContentControl Focusable="False" x:Name="Contenido" HorizontalAlignment="Center" Margin="0" 
							Content="{TemplateBinding Content}" VerticalAlignment="Center" />
					</Grid>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="true">
							<Setter Property="Opacity" Value="0.85" />
						</Trigger>
						<Trigger Property="IsEnabled" Value="false">
							<Setter Property="Opacity" Value="0.25"/>
						</Trigger>
						 <Trigger Property="local:CretaProperties.IsButtonSelected" Value="true">
                            <Setter Property="Opacity" Value="0.4"/>                           
                            <Setter Property="IsEnabled" TargetName="GridButton" Value="true"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
	
	<Style x:Key="CretaButtonTransparentC4" TargetType="{x:Type Button}">
		<Setter Property="FocusVisualStyle" Value="{StaticResource ButtonFocusVisual}"/>
		<Setter Property="HorizontalContentAlignment" Value="Center"/>
		<Setter Property="VerticalContentAlignment" Value="Center"/>
		<Setter Property="Padding" Value="1"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type Button}">
					<Grid x:Name="GridButton" Cursor="Hand" Background="Transparent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="true">
							<Setter Property="Opacity" Value="0.85" />
						</Trigger>
						<Trigger Property="IsEnabled" Value="false">
							<Setter Property="Opacity" Value="0.25"/>
						</Trigger>
						 <Trigger Property="local:CretaProperties.IsButtonSelected" Value="true">
                            <Setter Property="Opacity" Value="0.4"/>                           
                            <Setter Property="IsEnabled" TargetName="GridButton" Value="true"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
	
	<Style x:Key="CretaButtonContentSelectedC4" TargetType="{x:Type Button}">
		<Setter Property="FocusVisualStyle" Value="{StaticResource ButtonFocusVisual}"/>
		<Setter Property="HorizontalContentAlignment" Value="Center"/>
		<Setter Property="VerticalContentAlignment" Value="Center"/>
		<Setter Property="Padding" Value="1"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type Button}">
					<Grid x:Name="GridButton" Cursor="Hand">
						<ContentControl Focusable="False" x:Name="Contenido" HorizontalAlignment="Center" Margin="0" 
							Content="{TemplateBinding Content}" VerticalAlignment="Center" />
						<Rectangle x:Name="RectangleMouseOver" Stroke="{DynamicResource CretaprintC4_GrisClaro_Trazos_br}" Opacity="0.5" Visibility="Collapsed"
							StrokeThickness="2" RadiusX="1" RadiusY="1"/>
						<Rectangle x:Name="RectangleSelected" Stroke="{DynamicResource CretaprintC4_efi_azul_br}" Visibility="Collapsed"
							StrokeThickness="2" RadiusX="1" RadiusY="1"/>
					</Grid>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="true">
							<Setter Property="Visibility" TargetName="RectangleMouseOver" Value="Visible"/>
						</Trigger>
						<Trigger Property="IsEnabled" Value="false">
							<Setter Property="Opacity" Value="0.25"/>
						</Trigger>
						 <Trigger Property="Tag" Value="Selected">
                            <Setter Property="Visibility" TargetName="RectangleSelected" Value="Visible"/>                          
                            <Setter Property="IsEnabled" TargetName="GridButton" Value="true"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
</ResourceDictionary>