<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
	xmlns:local="clr-namespace:CretaBase"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	mc:Ignorable="d">
    <!-- Resource dictionary entries should be defined here. -->
    <Style x:Key="CretaButtonThin" TargetType="{x:Type Button}">
        <Setter Property="FocusVisualStyle" Value="{DynamicResource CretaFocusVisualStyle}" />
        <Setter Property="Background" Value="{StaticResource ButtonNormalBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ButtonNormalBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <ControlTemplate.Resources>
                        <Storyboard x:Key="Red">
                            <ColorAnimationUsingKeyFrames RepeatBehavior="Forever" Storyboard.TargetProperty="(Shape.Fill).(SolidColorBrush.Color)" Storyboard.TargetName="rectangle1">
                                <DiscreteColorKeyFrame KeyTime="0" Value="Red"/>
                                <DiscreteColorKeyFrame KeyTime="0:0:1.1" Value="White"/>
                                <DiscreteColorKeyFrame KeyTime="0:0:2" Value="Red"/>
                            </ColorAnimationUsingKeyFrames>
                            <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(TextElement.Foreground).(SolidColorBrush.Color)" Storyboard.TargetName="textBlock">
                                <DiscreteColorKeyFrame KeyTime="0" Value="{StaticResource Cretaprint_Negro}"/>
                                <EasingColorKeyFrame KeyTime="0:0:2" Value="{StaticResource Cretaprint_Negro}"/>
                            </ColorAnimationUsingKeyFrames>
                            <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(TextElement.Foreground).(SolidColorBrush.Color)" Storyboard.TargetName="textBlock1">
                                <DiscreteColorKeyFrame KeyTime="0" Value="{StaticResource Cretaprint_Negro}"/>
                                <EasingColorKeyFrame KeyTime="0:0:2" Value="{StaticResource Cretaprint_Negro}"/>
                            </ColorAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="Yellow">
                            <ColorAnimationUsingKeyFrames RepeatBehavior="Forever" Storyboard.TargetProperty="(Shape.Fill).(SolidColorBrush.Color)" Storyboard.TargetName="rectangle1">
                                <EasingColorKeyFrame KeyTime="0" Value="#FFC8C832"/>
                            </ColorAnimationUsingKeyFrames>
                            <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(TextElement.Foreground).(SolidColorBrush.Color)" Storyboard.TargetName="textBlock">
                                <EasingColorKeyFrame KeyTime="0" Value="{StaticResource Cretaprint_Negro}"/>
                            </ColorAnimationUsingKeyFrames>
                            <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(TextElement.Foreground).(SolidColorBrush.Color)" Storyboard.TargetName="textBlock1">
                                <EasingColorKeyFrame KeyTime="0" Value="{StaticResource Cretaprint_Negro}"/>
                            </ColorAnimationUsingKeyFrames>
                        </Storyboard>
                    </ControlTemplate.Resources>
                    <Grid Margin="0" Cursor="Hand">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.75*"/>
                            <ColumnDefinition Width="0.25*"/>
                        </Grid.ColumnDefinitions>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Shape.Stroke).(GradientBrush.GradientStops)[0].(GradientStop.Color)" Storyboard.TargetName="rectangle">
                                            <EasingColorKeyFrame KeyTime="0" Value="{StaticResource Cretaprint_Naranja}"/>
                                        </ColorAnimationUsingKeyFrames>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Shape.Stroke).(GradientBrush.GradientStops)[1].(GradientStop.Color)" Storyboard.TargetName="rectangle">
                                            <EasingColorKeyFrame KeyTime="0" Value="{StaticResource Cretaprint_Naranja}"/>
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Shape.Stroke).(GradientBrush.GradientStops)[1].(GradientStop.Color)" Storyboard.TargetName="rectangle">
                                            <EasingColorKeyFrame KeyTime="0" Value="#FFFF6000"/>
                                        </ColorAnimationUsingKeyFrames>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Shape.Stroke).(GradientBrush.GradientStops)[0].(GradientStop.Color)" Storyboard.TargetName="rectangle">
                                            <EasingColorKeyFrame KeyTime="0" Value="{StaticResource Cretaprint_Naranja}"/>
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled"/>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="FocusStates">
                                <VisualState x:Name="Unfocused"/>
                                <VisualState x:Name="Focused"/>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Rectangle x:Name="rectangle1" RadiusY="0" RadiusX="0" StrokeThickness="2" Stroke="{x:Null}" Grid.ColumnSpan="2" Margin="0,1,1,0">
                            <Rectangle.Fill>
                                <SolidColorBrush Color="{DynamicResource CretaPrint_BorderWindow}"/>
                            </Rectangle.Fill>
                        </Rectangle>
                        <Rectangle x:Name="rectangle" RadiusY="0" StrokeThickness="2" Fill="{x:Null}" Margin="0,1,1,0" Grid.ColumnSpan="2" RadiusX="0">
                            <Rectangle.Effect>
                                <DropShadowEffect Direction="-90" Color="Black" ShadowDepth="1"/>
                            </Rectangle.Effect>
                            <Rectangle.Stroke>
                                <LinearGradientBrush EndPoint="0.552,-0.555" StartPoint="0.484,0.992">
                                    <GradientStop Color="#FF5A5A5A" Offset="0"/>
                                    <GradientStop Color="#FF999999" Offset="1"/>
                                </LinearGradientBrush>
                            </Rectangle.Stroke>
                        </Rectangle>
                        <TextBlock x:Name="textBlock" HorizontalAlignment="Right" Margin="10,1,2,0" Style="{DynamicResource CretaTextBlock}" Text="{TemplateBinding Content}" Grid.Column="0" VerticalAlignment="Center" FontSize="10.667">
							<TextBlock.Foreground>
								<SolidColorBrush Color="{DynamicResource Cretaprint_Naranja}"/>
							</TextBlock.Foreground>
                        </TextBlock>
                        <TextBlock x:Name="textBlock1" HorizontalAlignment="Left" Margin="0,1,10,0" Style="{DynamicResource CretaTextBlock}" Text="{TemplateBinding Tag}" Grid.Column="1" VerticalAlignment="Center" FontSize="10.667">
							<TextBlock.Foreground>
								<SolidColorBrush Color="{DynamicResource Cretaprint_Naranja}"/>
							</TextBlock.Foreground>
                        </TextBlock>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="local:CretaProperties.BlinkYellow" Value="True">
                            <Trigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="Yellow_BeginStoryboard"/>
                            </Trigger.ExitActions>
                            <Trigger.EnterActions>
                                <BeginStoryboard x:Name="Yellow_BeginStoryboard" Storyboard="{StaticResource Yellow}"/>
                            </Trigger.EnterActions>
                        </Trigger>
                        <Trigger Property="local:CretaProperties.BlinkRed" Value="True">
                            <Trigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="Red_BeginStoryboard"/>
                            </Trigger.ExitActions>
                            <Trigger.EnterActions>
                                <BeginStoryboard x:Name="Red_BeginStoryboard" Storyboard="{StaticResource Red}"/>
                            </Trigger.EnterActions>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="true"/>
                        <Trigger Property="ToggleButton.IsChecked" Value="true"/>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="#ADADAD"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>